/*
 * read_command.c
 *
 *  Created on: Sep 13, 2016
 *      Author: Kevin
 */

#include "Project01.h"

void read_command(){
	setbuf(stdout, NULL);
	scanf("%s", input);
	if (strncmp(input, "quit", 256) == 0){
		printf("Exiting Program...");
		return 0;
	}
}
